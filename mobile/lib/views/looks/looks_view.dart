import 'package:flutter/material.dart';

/// Criação manual de looks e sugestões automáticas (MVP #3, #4).
class LooksView extends StatelessWidget {
  const LooksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Looks')),
      body: const Center(child: Text('TODO: montagem manual e sugestões de looks')),
    );
  }
}
